Concepts:

？ 无理数可以用二进制表示吗（用序列无限逼近？）
## Binary
#### 2.3 Decimal-to-Binary Conversion（two methods）
1. 


#### 2.4 Binary Arithmetic

## Arithmetic

## Codes
#### 1's complement (反码)
#### 2's complement (补码)
method1：1's complement后LSB（最低有效位）加一
method2：从右开始抄，抄到第一个1出现，左边都取反
补码的补码是自己

#### Signed Numbers（带符号数）
逐层进步的设计
1. Simplest way: Sign-Magnitude Form(the first bit for sign bit and the rest 7 for magnitude bits，0正1负)
2. 用反码：第一位依然是符号位，正原负反码。好处：算十进制时，直接位权相加后结果加一（符号位是负的）
3. 用补码：第一位依然是符号位，正原负补码。（根据上一条最后还要加一）算十进制时，直接位权相加（符号位权重负）
且1.2.种方法都有浪费（0有两种表示）范围是-127-127
3.的表示范围是-128-127
n位比特：表示2^n个数，范围：-2^（n-1）-- 2^（n-1）-1
补码算数很方便，符号位不影响可以直接加（尤其考虑下负数，把最高位扔掉那个）

？可以思考下原理（基本原理课件上有）

#### Floating-Point Numbers
scientific notation
manitisa（尾数）+ exponent（指数）+ sign（符号）
three forms：single-precision，double-precision，extended-precision
single-precision：（32bits)

| single-precision | sign(S) |                      exponent(F)                      | mantissa(E) |
| :--------------: | :-----: | :---------------------------------------------------: | :---------: |
|                  |    1    |                           8                           |     23      |
|                  |         | Biased (add 127 to actual exponent，making 0 included) |             |
| 指数1尾数0无穷大，0.0为0  |         |                actual range: -126-128                 |             |

Number = (-1)
*这个公式补全，然后有一页字节术语*

overflow问题


